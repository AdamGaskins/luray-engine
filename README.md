# Luray Engine - Lua + Raylib

Prealpha

Usage

```sh
luray init project # Scaffolds a new project
cd project
luray dev # Runs with hotreloading
luray export # Exports to all platforms (including web!) with one command
```
